initSidebarItems({"constant":[["BOOT_INFO",""],["HEAP_END",""],["HEAP_START","This is the address of the start of the kernel heap. The heap is 100 KiB."],["KERNEL_BASE","This is the base of the kernel address space. It starts at -2GB. We don't know how much memory the kernel image will take up when loaded into memory, so we leave quite a lot of space until the next statically mapped thing."],["LOCAL_APIC_CONFIG_PAGE",""],["P4_TABLE_ADDRESS","This address can be used to access the currently mapped P4 table, assuming the correct entry is recursively mapped properly."],["RECURSIVE_ENTRY","We use the 510th entry of the PML4 (P4) to access the page tables easily using the recursive paging trick. Any address that would use this entry can therefore not be used. This entry was picked because it places the unusable portion of the virtual address space between the userspace and kernel portions, which is less inconvienient than it being a hole."]]});