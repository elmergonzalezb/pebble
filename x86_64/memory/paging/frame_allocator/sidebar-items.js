initSidebarItems({"trait":[["FrameAllocator","`FrameAllocator` represents the `x86_64` crate's interface with the physical memory manager, allowing it to remain independent of the actual method used to track allocated frames. This allows us to use the crate from both the bootloader, where physical memory is managed by the UEFI's boot services, and in the kernel, where we manage it manually."]]});