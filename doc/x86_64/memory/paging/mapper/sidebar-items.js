initSidebarItems({"struct":[["Mapper","A `Mapper` allows you to change the virtual to physical mappings in a set of page tables. It relies on the set of page tables it represents being accessible through the current recursive mapping, and so this structure can only safely exist for either the currently mapped set of tables, or if the physical address of the P4 this `Mapper` refers to has been installed into the recursive entry of the currently mapped P4."]]});