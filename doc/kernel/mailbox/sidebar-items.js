initSidebarItems({"struct":[["Mailbox","A `Mailbox` is a kernel object that allows the kernel to send simple messages for a task to deal with. It employs a simple message-pumping system, where a queue of \"mails\" are kept in the kernel, and the task is expected to handle them by calling a blocking system call."]]});